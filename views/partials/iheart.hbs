<button type="button" class="btn btn-card favorite-btn" 
        data-type="{{contentType}}" 
        data-id="{{contentID}}">
  <i class="bi {{#if isFavorite}}bi-heart-fill{{else}}bi-heart{{/if}}"></i>
</button>