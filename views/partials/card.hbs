<div class="col">
    <div class="card mx-auto">
        <img src="{{card.img}}" class="card-img-top p-2 img-fluid" alt="Card Image">
        <div class="card-body text-start">
            <h4>{{card.title}}</h4>
            <p>{{card.description}}</p>
        </div>
    </div>
</div>